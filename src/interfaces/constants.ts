export interface Env {
  DEVELOPMENT: string,
  PRODUCTION: string,
};

export interface WeekDays {
  Sunday: number,
  Monday: number,
  Tuesday: number,
  Wednesday: number,
  Thursday: number,
  Friday: number,
  Saturday: number,
};

export interface Authentication {
  0: string,
  1: string,
};
